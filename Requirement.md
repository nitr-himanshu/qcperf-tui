The requirement is to create Terminal user interface using libqcperf To show a live graphical based Graphs for metric and values.

So when dui initiate At that time It should do Of lip qipper And fetch the capabilities After that It should have a default dashboard To show cpu uses Or any other matrix But it should give flexibility to the user to create Custom dashboard So user can select in particular dashboard which metric they want to profile And how they want Show So for the unit where its percentage we can use pie chart For others where we have Other units like MHZ frequencies And all that we can show using Line bar graph So the user can select which all metrics they want to So choose in that dashboard and once they choose They it will start showing all the capabilities User can select in particular for each Graph what could be the sampling and streaming rate and that should be fetched from the capability information so whenever User wants he can go to any dashboard and there should be option of edit Start And taking the snapshot Also it in dashboard it should be there like if user want to save the same data into the csv So even user switch from one dashboard to another So it should not stop the profiling So it is like two dashboard can run together as well But there should be an option in dashboard to stop the profiling The graphical window should be scalable and it should not like it should be like task manager in windows it should not be it should show the values like graphs of certain time. And user can select the colour colours there should be a default colours but your user can configure it And So the project should be compiled for Arm GNU compiler, android compiler, win microsoft arm 64
